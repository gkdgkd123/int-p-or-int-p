#include <stdio.h>
int main()
{
	
	int* p;
	int a = 9;
	p = &a;
	printf(" p=%p\n",p);
	printf("&a=%p\n",&a);
	//得出结论：应该理解为int* p，p是指针变量，int* 相当于一种类型
  //*p可理解为一个整数 *p = a;是能通过编译的，但有很大可能导致程序崩溃（p需要初始化，即指向一个地址，例如：p = &a；）
  
	
	
	
	
	
	return 0;
 } 
